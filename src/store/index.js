import { createStore } from "vuex";
import { createApp } from "vue";

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {},
});

const app = createApp({});
app.use();
